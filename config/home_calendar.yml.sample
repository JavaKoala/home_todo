default: &default
  enabled: <%= ENV.fetch("HOME_CALENDAR_ENABLED") { false } %>
  url: <%= ENV.fetch("HOME_CALENDAR_URL") { 'http://localhost:3001/api/v1' } %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
